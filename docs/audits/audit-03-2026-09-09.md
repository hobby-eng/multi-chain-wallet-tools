# Repeat-audit remediation — 2026-09-09

Reviewed baseline: `94eb3374cc6d3e67a578d459891d0f1e7d65ecab`, release metadata 0.1.3. The repeat first-party review reported six P2 correctness/validation findings and two P3 documentation findings. This record describes their corrections; it is not an independent cryptographic certification or a dependency audit.

| Finding | Correction | Focused evidence |
| --- | --- | --- |
| R01 — token supply exported as credits | CSV/XLSX token rows leave DASH amount fields empty and preserve token ID, supply and decimals as metadata. JSON remains token-typed. | Real CSV, generated XLSX XML and JSON assertions in Activity Viewer export tests. |
| R02 — Ethereum case duplicates | Canonical address identity is deduplicated before lookup and aggregation. | One account in different case forms plus a distinct control yields two queries and 2 ETH, not three. |
| R03 — Identity history integrity | Summary counts, stable page totals and collection identities are checked. Missing counts are explicitly unverified; bounded results retain display-limit warnings. Scanner transfer summaries use the same integrity guard. | 150 unique records, overlap, missing/unknown count, changed count, premature end, resource revision duplicate, and legitimate transfer legs. |
| R04 — stale cross-query state | Removed retained Bitcoin balance state and retained Platform Explorer synchronization responses. First-party HTTP bypasses browser caching. | Same service observes 100 then 0 satoshis; a new query rejects a newly unsynchronized Explorer. |
| R05 — partial Orchard accounting | Completion context reaches note presentation, section metrics, aggregate, CSV/JSON and seed/public scan status. Unknown balance is nullable; observed note value and known spend remain available. | Same ledger rendered partial/complete and exported; observed spend stays spent; empty IVK scan remains unavailable; both controller source modes report incomplete. |
| R06 — address checksums | Shared Bitcoin Base58Check/witness decoders and Ethereum EIP-55 validation run in UI and network boundary. Complete batch screening precedes networking; detected secrets are erased. | Corrupted checksum, wrong network, witness version/checksum/length mismatch, uppercase positive control, Ethereum checksum cases and zero-request private-input test. |
| D01 — native Rust selection | Native commands explicitly select the pinned installed toolchain with `RUSTUP_TOOLCHAIN`. | Instructions now distinguish installing from selecting the toolchain. |
| D02 — documentation drift | Corrected immutable audit metadata description, recorded historical browser acceptance, removed future-bundle wording, and documented current input/completeness/export behavior. | Metadata/link consistency checks; source-to-prose review. |

The Identity count comparison was checked against the public Explorer DAO at commit `ddc4c24a482906d2e6a15b13419786586cac3807`: summary transactions/transfers and unfiltered latest document/contract records match the corresponding collection scopes. The transfer response has no populated stable row ID. Identical movement tuples on different pages are therefore treated as ambiguous and rejected; distinct legitimate legs with the same transaction hash remain separate. Provider history is auxiliary data and count reconciliation cannot prove inclusion of every chain event.

Only cross-query wallet/index result caches were removed. Per-scan deduplication, the current public report and SDK connection state remain in memory. A new explicit run fetches state again; remote index freshness remains the provider's responsibility. The Secret Vault sandbox, CSP, fixed network operations, viewing-key locality and absence of spending/broadcast APIs are unchanged.

Targeted tests and a TypeScript check accompany these fixes. Final source/native verification and rebuilt-file browser acceptance must record the corrected commit and artifact hashes. The previous 12/12 browser report belongs to `94eb337`, not these changes. No fresh browser, Docker, live-provider or full native acceptance is claimed by this document. Detailed command logs and final commit mapping are kept with the local remediation handoff.
